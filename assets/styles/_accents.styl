.tm-crosshair
  $size = 6rem
  height $size
  width ($size / 2)
  position relative
  display block
  border-left $px solid

  &::before,
  &::after
    content ""
    display block
    position relative

  &::before
    border-top $px solid
    left -100%
    margin-left -($px*2)
    width $size
    top ($size / 2)

  &::after
    $circle = (1rem - $px)
    height $circle
    width $circle
    border-radius 50%
    border $px solid
    top 50%
    left ($circle/-2 - $px)
    margin-top ($circle/-2 - $px)
